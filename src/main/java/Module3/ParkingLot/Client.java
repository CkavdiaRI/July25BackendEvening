package Module3.ParkingLot;

public class Client {
    // Create all repositories
    // Then the service
    // Then the controller
    // Then ask the controller to get your ticket
}

// Feature -> Ticket generation

// Input  : LicensePlate, vehicleType, ownerName, gateId
// Output : All ticket details but most importantly parkingSlot